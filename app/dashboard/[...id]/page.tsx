import React from 'react';

const PageLink = ({ params }: { params: { slug: string } }) => {
    return (
        <div>
            <h1>page link</h1>
        </div>
    );
};

export default PageLink;
